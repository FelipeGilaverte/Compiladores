#include <stdio.h>


int main() {
    int x = 10; // variável x
    int y = 20; // variável y

    // soma
    int resultado = x + y;

    /* outro comentário */
    printf("Resultado = %d\n", resultado);

    return 0;
}